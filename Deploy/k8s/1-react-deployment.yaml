apiVersion: apps/v1
kind: Deployment
metadata:
  name: react-landing-app-deployment
  namespace: dev
  labels:
    app: react-landing-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: react-landing-app
  template:
    metadata:
      labels:
        app: react-landing-app
    spec:
      containers:
        - name: react-landing-app
          image: panchanandevops/react-landing-page:v1.0.0
          resources:
            limits:
              memory: "512Mi"
              cpu: "500m"
            requests:
              memory: "256Mi"
              cpu: "250m"
          ports:
            - containerPort: 8080
